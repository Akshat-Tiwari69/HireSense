# Railway Nixpacks configuration for frontend (Node)

[phases.install]
commands = [
  "npm ci"
]

[phases.build]
# Build the production bundle
commands = [
  "npm run build"
]

[start]
cmd = "npm start"

[variables]
NIXPACKS_NODE_VERSION = "20"
