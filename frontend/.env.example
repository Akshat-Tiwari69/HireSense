# Leave blank for the same-origin API used in development and production.
# Vite proxies /api and /socket.io locally; Caddy does the same in production.
# VITE_* values are public browser configuration and must never contain secrets.
VITE_API_BASE_URL=
